<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kenneth's To Do List</title>
</head>

<body>
    <% if (dayOfWeek==="Saturday" || dayOfWeek==="Sunday" ){ %>
    <h1 style="color: purple">
        <%= dayOfWeek %> To Do List!
    </h1>
    <% } else { %>
    <h1 style="color: green">
        <%= dayOfWeek %> To Do List!
    </h1>
    <% } %>
</body>

</html>